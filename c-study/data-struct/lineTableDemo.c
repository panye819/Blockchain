#include <stdio.h>

/**
    线性表：
        每个节点有一个直接前驱节点，有一个后继节点
        第一个节点没有前驱
        最后一个节点没有后驱
    顺序表：
        逻辑上是一个线性表，存储方式也是有序的
    链表：
        逻辑上是一个线性表，存储方式不是有序的
        分为单链表和双向链表
*/

int main()
{

    return 0;
}
